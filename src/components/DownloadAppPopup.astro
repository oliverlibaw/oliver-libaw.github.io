---
---
<div id="download-app-popup" class="popup-overlay">
  <div class="popup-content">
    <button id="download-app-close" class="close-button">&times;</button>
    <h3>Oliver's site is better in the app!</h3>
    <p>Don't hesitate! Download it today!</p>
    <div class="store-buttons">
      <a href="#" class="store-button"><img src="/images/app_download_ios.png" alt="Download on the App Store"></a>
      <a href="#" class="store-button"><img src="/images/app_download_android.png" alt="Get it on Google Play"></a>
    </div>
  </div>
</div>

<style>
  .popup-overlay {
    position: fixed;
    bottom: 2rem;
    right: 2rem;
    z-index: 1009;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
    opacity: 0;
    transform: translateY(20px);
    pointer-events: none;
    transition: opacity 0.5s ease, transform 0.5s ease;
  }

  .popup-overlay.visible {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
  }

  .popup-content {
    background: #fff;
    color: #000;
    border-radius: 12px;
    padding: 1.5rem;
    box-shadow: 0 8px 25px rgba(0,0,0,0.2);
    position: relative;
    max-width: 400px;
    text-align: center;
  }

  .close-button {
    position: absolute;
    top: 5px;
    right: 10px;
    font-size: 1.8rem;
    color: #ccc;
    background: none;
    border: none;
    cursor: pointer;
  }

  .popup-content h3 {
    margin: 0 0 0.5rem;
    font-size: 1.4rem;
  }

  .popup-content p {
    margin: 0 0 1.5rem;
    color: #555;
  }

  .store-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
  }

  .store-button {
    display: flex;
    align-items: center;
    text-decoration: none;
  }

  .store-button img {
    height: 50px;
  }
</style>

<style>
  @media (max-width: 768px) {
    .popup-content {
      max-width: 320px;
      padding: 1.25rem;
    }
    .popup-content h3 { font-size: 1.2rem; }
    .popup-content p { 
      font-size: 0.9rem; 
      margin-bottom: 1rem; 
    }
  }
</style>