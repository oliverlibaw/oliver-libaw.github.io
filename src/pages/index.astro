---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
---

<Layout title="Oliver Libaw's Personal Website" showPopups={true}>
	<style>
		body {
			margin: 0;
			font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto,
				'Helvetica Neue', Arial, sans-serif;
			background-color: #000;
			color: white;
			overflow-x: hidden;
		}

		.video-background {
			position: fixed;
			top: 50%;
			left: 50%;
			width: 100vw;
			height: 56.25vw; /* 16:9 aspect ratio */
			min-width: 100%;
			min-height: 100%;
			transform: translate(-50%, -50%);
			z-index: -2; 
		}

		.video-overlay {
			position: fixed;
			top: 0;
			left: 0;
			width: 100%;
			height: 100%;
			background-color: #000;
			opacity: 0;
			z-index: -1;
			transition: opacity 0.3s ease;
		}

		.video-background iframe {
			width: 100%;
			height: 100%;
		}
		
		.hero {
			height: 100vh;
			position: relative;
			display: flex;
			justify-content: center;
			align-items: center;
		}

		@keyframes flashy-colors {
			0% { background-color: #ff4136; transform: scale(1); }
			20% { background-color: #f012be; transform: scale(1.05); }
			40% { background-color: #00ff00; transform: scale(1); }
			60% { background-color: #ffff00; transform: scale(1.05); }
			80% { background-color: #00ffff; transform: scale(1); }
			100% { background-color: #ff4136; transform: scale(1.05); }
		}

		@keyframes spin {
			from {
				transform: rotate(0deg);
			}
			to {
				transform: rotate(360deg);
			}
		}

		.enshittify-button {
			position: fixed;
			top: 250px;
			right: 10px;
			z-index: 1000;
			padding: 3rem 3.5rem;
			font-size: 0.9rem;
			font-weight: bold;
			color: black;
			text-shadow: 1px 1px 2px white;
			border: none;
			cursor: pointer;
			clip-path: polygon(50% 0%, 61% 35%, 98% 35%, 68% 57%, 79% 91%, 50% 70%, 21% 91%, 32% 57%, 2% 35%, 39% 35%);
			animation: flashy-colors 2s infinite linear;
			text-align: center;
			line-height: 1.2;
		}

		.enshittify-button:hover {
			animation: flashy-colors 2s infinite linear, spin 1s infinite linear;
		}

		.de-enshittify-button {
			position: fixed;
			top: 250px;
			right: 10px;
			z-index: 1000;
			width: 100px;
			height: 100px;
			padding: 0;
			font-size: 0.8rem;
			font-weight: bold;
			color: black;
			background-color: white;
			border: none;
			border-radius: 50%;
			cursor: pointer;
			transition: background-color 0.2s, transform 0.2s, color 0.2s;
			display: flex;
			align-items: center;
			justify-content: center;
			text-align: center;
		}

		.de-enshittify-button:hover {
			background-color: #eee;
			transform: scale(1.05);
		}

		.details-section {
			background-color: #000;
			color: white;
			padding: 8rem 4rem;
			position: relative;
			z-index: 5;
		}

		.details-grid {
			display: grid;
			grid-template-columns: 1fr;
			gap: 5rem;
			max-width: 1400px;
			margin: 0 auto;
		}

		@media (min-width: 768px) {
			.details-grid {
				grid-template-columns: repeat(2, 1fr);
			}
			.column:last-child:nth-child(odd) {
				grid-column: 1 / -1;
				display: flex;
				flex-direction: column;
				align-items: center;
			}
			.column:last-child:nth-child(odd) h2,
			.column:last-child:nth-child(odd) p {
				text-align: center;
			}			
		}

		.column {
			text-align: center;
		}

		.column-image {
			width: 250px;
			height: 250px;
			border-radius: 50%;
			object-fit: cover;
			margin-bottom: 2rem;
		}

		.column h2 {
			font-size: 1.5rem;
			font-weight: 500;
			margin-top: 0;
			margin-bottom: 1.5rem;
			border-bottom: 1px solid rgba(255,255,255,0.2);
			padding-bottom: 0.75rem;
			text-align: left;
		}

		.column p {
			font-size: 1rem;
			line-height: 1.7;
			font-weight: 300;
			opacity: 0.9;
			margin: 0 0 1em;
			max-width: 70ch;
			text-align: left;
		}

	</style>
	<div class="hero">
		<div class="video-background">
			<div class="video-overlay" id="video-overlay"></div>

			<iframe
				src="https://player.vimeo.com/video/836697875?h=8c8e024462&autoplay=1&loop=1&muted=1&background=1"
				frameborder="0"
				allow="autoplay; fullscreen; picture-in-picture"
				allowfullscreen
				title="Background video of abstract shapes"></iframe>
		</div>
		<Header />
		<div class="enshittify-container">
			<button id="enshittify-button" class="enshittify-button">Enshittify<br>This Site!</button>
			<button id="de-enshittify-button" class="de-enshittify-button" style="display: none;">De-enshittify</button>
		</div>
	</div>
	<section class="details-section">
		<div class="details-grid">
			<div class="column" id="content-box-1">
				<img src="/furiosa_rngd_cards.avif" alt="FuriosaAI promotional cards" class="column-image" />
				<h2>Global PR and Editorial at FuriosaAI</h2>
				<p>In my current role as Global Lead for PR and Corporate Communications, I oversee global PR efforts, strategic messaging and thought leadership, and also lead content for major announcements. Reporting directly to the CEO of this 180-person startup, I am responsible for explaining Furiosa's technology to both expert and nonexpert audiences and showing why Furiosa is an exciting, important player in AI.</p>
				<p>Furiosa is a semiconductor startup making high-performance, energy-efficient AI chips. In 2025 it became the first Korean tech "unicorn."</p>
				<p>My work for Furiosa has led to coverage by Bloomberg, Reuters, TechCrunch, EE Times, and many other media outlets.</p>
			</div>
			<div class="column">
				<img src="/dino_image.webp" alt="Dinosaur illustration" class="column-image" />
				<h2>Launched and Managed the Meta AI Blog</h2>
				<p>During my five years at Meta/Facebook, I oversaw the Meta AI blog, aimed at an expert audience of researchers, engineers, tech reporters, and policy makers. It aims to explain what Meta AI is building, why it’s noteworthy, and what impact it'll have. </p>
				<p>Under my direction, we announced Meta AI’s first Llama large language model and Make-A-Scene generative AI work, among many other projects. </p>
				<p>In this role I worked closely with a broad cross-functional team including legal, policy, PR, research, and senior leadership (Chief AI Scientist Yann LeCun, Meta/Facebook Chief Technology Officer Mike Schroepfer, and VP of AI research Joelle Pineau).</p>
			</div>
			<div class="column">
				<img src="/mexico_city_times.webp" alt="Mexico City Times newspaper" class="column-image" />
				<h2>20 Years of Media and Journalism Experience</h2>
				<p>Before joining Facebook/Meta, I oversaw a general-audience lifestyle channel at Yahoo, where I grew monthly unique users from 3 to 9 million. </p>
				<p>Prior to working at Yahoo, I was a reporter and editor for the ABC News website, among other roles in media and journalism. </p>
				<p>I have Master’s degree in philosophy from Cambridge University and a Bachelor’s degree from Pomona College.</p>
			</div>
			<div class="column">
				<img src="/code_example.webp" alt="Example of code" class="column-image" />
				<h2>Hands-On Experience With Machine Learning</h2>
				<p>I've completed several courses in machine learning, including Andrew Ng's Coursera/DeepLearning.AI specialization. </p>
				<p>As part of these courses I've built various and trained various models for computer vision and natural language processing tasks. I've used common machine learning frameworks such as PyTorch and TensorFlow and I've developed a solid grounding in Python.</p>
			</div>
	
			<div class="column">
				<img src="/cozmos-wall.jpeg" alt="Example of code" class="column-image" />
			 	<h2>AI-Powered Artwork at COZMOS Gallery in NYC</h2>
				<p>I'm pleased to have an AI art project on display at COZMOS Gallery on NYC's Lower East Side.</p>
				<p>There's a lottttt of AI-generated art these days. This project tries to take a different tack: Instead of generating an image from a prompt, the AI invents the context for a (really uninteresting) real-world object.</p>
				<p>The project uses a language model, a Raspberry Pi-powered display and a piece of silver duct tape. </p>
				<p>The language model invented ~200 museum-style descriptions of a made-up artwork (the duct tape stuck to a wall) and those descriptions cycle through the Raspberry Pi display. The LLM's descriptions (IMO) are interesting, mostly fairly plausible (the Claude Monet one excepted), and kind of unsettling.</p>
			</div>
		</div>
	</section>
</Layout>

<script>
	document.addEventListener('DOMContentLoaded', () => {
		// Scroll-based video overlay fade
		const overlay = document.getElementById('video-overlay');
		if (overlay) {
			document.addEventListener('scroll', () => {
				const heroHeight = window.innerHeight;
				const scrollPosition = window.scrollY;
				const fadeStart = heroHeight * 0.25;
				const fadeEnd = heroHeight * 0.75;
				const opacity = Math.max(0, Math.min(1, (scrollPosition - fadeStart) / (fadeEnd - fadeStart)));
				overlay.style.opacity = opacity.toString();
			});
		}

		// "Enshittify" button logic
		const enshittifyButton = document.getElementById('enshittify-button');
		const deEnshittifyButton = document.getElementById('de-enshittify-button');

		if (enshittifyButton && deEnshittifyButton) {
			enshittifyButton.addEventListener('click', () => {
				document.dispatchEvent(new CustomEvent('show-gdpr'));
				enshittifyButton.style.display = 'none';
				deEnshittifyButton.style.display = 'block';
			}, { once: true });

			deEnshittifyButton.addEventListener('click', () => {
				// The simplest and most robust way to reset the state is to just reload the page.
				location.reload();
			});
		}


		// Popup sequence listeners
		document.addEventListener('gdpr-closed', () => {
			document.dispatchEvent(new CustomEvent('show-newsletter'));
		});

		document.addEventListener('newsletter-closed', () => {
			document.dispatchEvent(new CustomEvent('show-chatbot'));
		});

		document.addEventListener('chatbot-step1-closed', () => {
			document.dispatchEvent(new CustomEvent('show-follow-social'));
		});

		document.addEventListener('follow-social-closed', () => {
			document.dispatchEvent(new CustomEvent('show-chatbot-step2'));
		});

		document.addEventListener('chatbot-closed', () => {
			document.dispatchEvent(new CustomEvent('show-survey'));
		});

		document.addEventListener('survey-closed', () => {
			document.dispatchEvent(new CustomEvent('show-spin-wheel'));
		});

		document.addEventListener('spin-wheel-closed', () => {
			document.dispatchEvent(new CustomEvent('show-download-app'));
		});

		document.addEventListener('download-app-closed', () => {
			document.dispatchEvent(new CustomEvent('show-qr-code'));
		});

		document.addEventListener('qr-code-closed', () => {
			document.dispatchEvent(new CustomEvent('show-gdpr'));
		});
	});
</script>